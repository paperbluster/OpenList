services:
  openlist:
    build:
      context: ..
      dockerfile: OpenList/Dockerfile
    restart: always
    network_mode: host
    volumes:
      - './data:/opt/openlist/data'
    environment:
      - TZ=Asia/Shanghai
    container_name: openlist
